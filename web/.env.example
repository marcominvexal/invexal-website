# cPanel / mailbox SMTP — used by /api/contact (Book a Demo + Contact forms)
# Copy to .env.local and fill in real values. Never commit .env.local.

SMTP_HOST=mail.invexal.com
SMTP_PORT=465
SMTP_USER=marcom@invexal.com
SMTP_PASS=your-cpanel-email-password
SMTP_FROM=marcom@invexal.com
CONTACT_TO=marcom@invexal.com
